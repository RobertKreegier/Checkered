{
  "name": "checkered",
  "version": "0.1.0",
  "type": "module",
  "scripts": {
    "test": "node --test 'test/*.test.js'"
  },
  "dependencies": {
    "jsdom": "^30.0.1"
  },
  "description": "A generic engine for games played on a grid."
}
